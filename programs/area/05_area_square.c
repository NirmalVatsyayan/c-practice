#include<stdio.h>
#include<conio.h>

int main(){

    float area, side;
    printf("Enter length of a side : ");
    scanf("%f", &side);

    area = side * side;
    printf("Area of square is %f ", area);

}
